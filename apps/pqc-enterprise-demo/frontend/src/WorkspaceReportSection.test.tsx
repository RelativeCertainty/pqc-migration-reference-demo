import { render, screen, cleanup } from '@testing-library/react';
import { afterEach, expect, test } from 'vitest';
import { WorkspaceReportSection } from './WorkspaceReportSection';
import { DiscoveryReportSection } from './DiscoveryReportSection';
afterEach(cleanup);
const content = { phase: 'phase1', coverageRows: [{ familyId: 'traffic', areaId: 'area-03', name: 'Traffic', observations: 3, achievedDepth: 'bounded_inventory' }], workspace: { hasActivity: true, populationStatement: 'Two of three reported deployments.', populationBasis: 'Enterprise population unknown.', receipts: [{ requestId: 'request-1', familyId: 'traffic' }], bundles: [{ status: 'admitted' }], consequences: [{ id: 'c1', requestId: 'request-1', phase1Conclusion: 'Configuration and observation differ.', limitation: 'Blocked edge remains unknown.', nextDecision: 'Reconcile client scope.' }] } };
test('shows frozen workspace conclusions, source counts and limits in the reading surface', () => { render(<WorkspaceReportSection content={content} />); expect(screen.getByText('Configuration and observation differ.')).toBeTruthy(); expect(screen.getByText('Blocked edge remains unknown.')).toBeTruthy(); expect(screen.getByRole('navigation', { name: 'Discovery-domain report contents' })).toBeTruthy(); });
test('shows reviewed Phase2 context without substituting a different scenario', () => { render(<WorkspaceReportSection content={{ ...content, phase: 'phase2', scenarioRows: [{ familyId: 'traffic', scenario: 'Conditional loss of compatibility.', protectedInformationLifetime: 'Seven years; fictional owner statement.', businessReviewedBy: 'Simulated business reviewer', analysisQualification: 'Client population unresolved.' }] }} />); expect(screen.getByText('Conditional loss of compatibility.')).toBeTruthy(); expect(screen.getByText('Seven years; fictional owner statement.')).toBeTruthy(); expect(screen.queryByText('Configuration and observation differ.')).toBeNull(); });
test('legacy discovery cannot falsely deny admitted workspace records', () => { render(<DiscoveryReportSection content={{ ...content, discovery: { hasActivity: true, summary: 'Contributions only.', responses: [], investigations: [], standards: [], limitations: [], nextDecision: 'Review.' } }} onInspect={() => {}} />); expect(screen.getByText(/Reviewed technical records admitted through Assessment work/)).toBeTruthy(); expect(screen.queryByText(/No reviewed technical records from discovery have been admitted/)).toBeNull(); });
test('does not fabricate workspace material for legacy reports', () => { const { container } = render(<WorkspaceReportSection content={{}} />); expect(container.textContent).toBe(''); });
